<%= link_to "Wikinimous", articles_path %>
<%= link_to "New article", articles_path %>

<article>
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article.id) %></h2>
    <p><%= article.content %></p>
  <% end %>
</article>
